import {CSSProperties} from "../../../../types/common.ts";

export default {
    container: {
       minWidth: '100%',
        display: 'flex',
        justifyContent: 'center',
        backgroundColor: '#fff',
    }
} as CSSProperties<'container'>;

