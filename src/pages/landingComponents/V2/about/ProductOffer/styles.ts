import {CSSProperties} from "../../../../../types/common.ts";

export default {
    container: {
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        flex: 10,
    },
} as CSSProperties<'container'>;