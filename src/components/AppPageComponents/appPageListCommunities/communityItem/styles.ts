import {CSSProperties} from "../../../../types/common.ts";

export default {
    mainContainer: {
        display:"flex",
        justifyContent:"flex-start",
        alignItems:"center",
        gap:'16px',
    },

} as CSSProperties <'mainContainer' >;