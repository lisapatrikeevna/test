import {CSSProperties} from "../../../types/common.ts";

export default {
    container: {
        position: "fixed",
        width:"100%",
        zIndex: 1000
    },

} as CSSProperties<'container'>;